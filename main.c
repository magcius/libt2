
/* gcc requires a file with a .c extension to compile to an executable,
 * unless there's a flag I didn't find... Just use the empty file for
 * now -- the actual implementation is in each .h file. */
 